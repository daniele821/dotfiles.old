# firefox profile backups (password protected)

run the following command, after inserting actual values where `<>` is present

```
cd ~/.mozilla/firefox
zip -er <DD_MM_YYYY-firefox.zip> <name_profile_dir>
```

then insert the password
